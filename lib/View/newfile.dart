import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Newpage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('MyPage')),
      body: Container(child: Text("sas")),
    );
  }
}
